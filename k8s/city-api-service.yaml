apiVersion: v1
kind: Service
metadata:
  name: city-api
  namespace: global-city-streaming
spec:
  selector:
    app: city-api
  ports:
  - name: http
    port: 8003
    targetPort: 8003
  type: LoadBalancer 